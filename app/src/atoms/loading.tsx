import CircularProgress from "@mui/material/CircularProgress";

import * as Styled from "./loading.styled";

export default () => (
  <Styled.Container>
    <CircularProgress />
  </Styled.Container>
);
