//import { PublicKey } from "@solana/web3.js";

export enum Commands {
  CancelWithdrawals,
  DeleteTestPair,
  DeleteTestPool,
  GetOutstandingAmount,
  Init,
}

//export async function init(
//signers: PublicKey[],
//minSignatures: number
//): Promise<any> {
//console.log(signers, minSignatures);
//}
